# prod build: เรียก API/socket แบบ same-origin แล้วให้ Cloudflare Worker (worker.js)
# reverse-proxy /api + /socket.io ไป backend (Render) เอง — cookie จะเป็น first-party
# ค่าว่าง = axios baseURL เป็น "/api" และ socket.io เชื่อม same-origin
VITE_API_URL=
